struct Point {
    int x;
    int y;
};

struct Point make(int a, int b) {
    struct Point p;
    p.x = a;
    p.y = b;
    return p;
}

int sum_coords(struct Point p) {
    return p.x + p.y;
}

int main(void) {
    struct Point q;
    q = make(3, 4);
    return sum_coords(q);
}
