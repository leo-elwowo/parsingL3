int i;
int somme;

int main(void) {
    i = 4;
    somme = 0;
    
    // Tant que i n'est pas égal à 0
    while (i) { 
        somme = somme + 10; // On ajoute 10 à la somme
        i = i - 1;          // On décrémente i
    }
    
    return somme; // Devrait renvoyer 40 !
}